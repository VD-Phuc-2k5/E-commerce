package com.dontwait.server.configuration;

// Spring Boot auto-configures Redis (LettuceConnectionFactory, StringRedisTemplate, RedisTemplate)
// from spring.data.redis.* properties in application.yml.
// No manual config needed.
